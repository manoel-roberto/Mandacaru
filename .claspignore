# Como rootDir é "src", todos os arquivos de código-fonte TypeScript, HTML e JSON dentro dele
# serão transpilados e enviados automaticamente pelo clasp v2.5.0.
# Nenhuma exclusão é necessária aqui para o conteúdo de src/
